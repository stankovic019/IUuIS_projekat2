<UserControl x:Class="NetworkService.Views.NetworkEntitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModel="clr-namespace:NetworkService.ViewModel"
             Focusable="True" Loaded="UserControl_Loaded">

    <UserControl.DataContext>
        <viewModel:NetworkEntitiesViewModel/>
    </UserControl.DataContext>

    <UserControl.InputBindings>
        <KeyBinding Key="R" Modifiers="Control" Command="{Binding ResetCommand}" />
    </UserControl.InputBindings>
    

    <Border Padding="10">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.08*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBlock Text="Total Entities: " FontSize="36" FontFamily="Courier New" FontWeight="DemiBold"/>
                <TextBlock Text="{Binding TotalEntities}" FontSize="36" FontFamily="Courier New" FontWeight="DemiBold"/>
            </StackPanel>

            <Grid Grid.Row="1">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="9*"/>
                    </Grid.RowDefinitions>


                    <Grid Grid.Row="0">
                        <StackPanel Orientation="Horizontal">
                            <ComboBox Height="20" Width="230" SelectedIndex="{Binding SelectedTypeIndex}" FontFamily="Courier New" ToolTip="List of measurement types" Margin="0 0 10 0">
                                <ComboBoxItem Content="Types..."/>
                                <ComboBoxItem Content="CableSensor"/>
                                <ComboBoxItem Content="DigitalManometer"/>
                            </ComboBox>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="&lt;" IsChecked="{Binding LessThanChecked}" FontSize="25" FontFamily="Courier New" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="0 0 5 0"/>
                                <RadioButton Content="=" IsChecked="{Binding EqualChecked}" FontSize="25" FontFamily="Courier New" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="0 0 5 0"/>
                                <RadioButton Content=">" IsChecked="{Binding GreaterThanChecked}" FontSize="25" FontFamily="Courier New" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="0 0 5 0"/>
                            </StackPanel>
                            <TextBox Width="100" Height="20" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" FontFamily="Courier New" ToolTip="Input value" Margin="0 0 10 0" VerticalAlignment="Center"/>

                            <ComboBox Height="20" Width="230" SelectedIndex="{Binding SelectedFilterIndex}" FontFamily="Courier New" ToolTip="Filter entities" Margin="0 0 10 0">
                                <ComboBoxItem Content="Filters..."/>
                                <ComboBoxItem Content="Normal Pressure"/>
                                <ComboBoxItem Content="High Pressure"/>
                                <ComboBoxItem Content="Low Pressure"/>
                            </ComboBox>
                            <Button Content="Reset" Height="20" Width="100" Command="{Binding ResetCommand}" FontFamily="Courier New" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="1">

                            <StackPanel>
                                <TextBlock Text=" History " FontSize="30" HorizontalAlignment="Center" FontFamily="Courier New" FontWeight="DemiBold"
                                           TextDecorations="Underline"/>

                                <ListBox Height="511" ScrollViewer.CanContentScroll="True" Margin="0 10 0 0" FontFamily="Courier New" FontWeight="DemiBold" ItemsSource="{Binding HistoryDtos}" SelectedItem="{Binding SelectedHistoryItem, Mode=TwoWay}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ActionName}" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Column="0">

                            <DataGrid x:Name="netEntTable" AutoGenerateColumns="False" Margin="0 0 10 0" HeadersVisibility="Column" Grid.Row="1" FontFamily="Courier New" VerticalContentAlignment="Center" Height="555" ItemsSource="{Binding ValvesView}" SelectedItem="{Binding SelectedValve, Mode=TwoWay}" >
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridRow">
                                        <Style.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#0078d7"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                                        </Style.Resources>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Validation}" Value="Low">
                                                <Setter Property="Background" Value="LightCoral"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Validation}" Value="High">
                                                <Setter Property="Background" Value="LightCoral"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="0.3*" FontFamily="Courier New" FontWeight="DemiBold" FontSize="16"/>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" FontFamily="Courier New" FontWeight="DemiBold" FontSize="16"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" FontFamily="Courier New" FontWeight="DemiBold" FontSize="16"/>
                                    <DataGridTextColumn Header="Last Measurement" Binding="{Binding MeasuredValue}" Width="0.2*" FontFamily="Courier New" FontWeight="DemiBold" FontSize="16"/>
                                    <DataGridTextColumn Header="Status" Binding="{Binding Validation}" Width="0.6*" FontFamily="Courier New" FontWeight="DemiBold" FontSize="16"/>
                                </DataGrid.Columns>
                            </DataGrid>

                        </Grid>
                        
                    </Grid>
                </Grid>

             
                
                
                
            </Grid>

        </Grid>
        
    </Border>
</UserControl>
